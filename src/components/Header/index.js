import React from 'react'

// styles
import styles from './styles.css'

const Header = () => (
  <div className={styles.container}>
    <span>This will be a Header.</span>
  </div>
)

export default Header
