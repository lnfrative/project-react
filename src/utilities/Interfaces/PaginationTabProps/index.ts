import { PaginationObject } from '@/utilities/Interfaces'

interface PaginationTabProps {
  paginationObject: PaginationObject,
  pathnameBase: string,
  pathname: string,
}

export default PaginationTabProps
