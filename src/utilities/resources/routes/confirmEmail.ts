import { Route } from 'interfaces'

const confirmEmail: Route = {
  base: '/confirm-email',
  middlewares: [],
  route: {
    path: '/confirm-email',
    params: {},
  },
  aliases: {},
}

export default confirmEmail
